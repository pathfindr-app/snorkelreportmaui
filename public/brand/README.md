Place your generated logo here as:

- /public/brand/snorkelreport-logo.svg (recommended)

You can also use PNG/WebP by updating BRAND_LOGO_SRC in:
- src/components/Layout/Header.jsx

The header automatically falls back to text if the logo file is missing.
